let String count = "Hello " + "World".
let int x = 5.
let float y = x + 10 + toInt(5.6).
x = 15.
multiply x by 1.
add 1 to y.
subtract 10 from x.
divide x by 2.

function test(int val){
    x = val.
    test2(5.0).
}

function test2(float val){
    y = val.
    
    function int(){
        test(1).
    }
}
test(100).
